
const donationButton = document.querySelector('.donation-button');
const paymentOptions = document.querySelector('.payment-options');
const paymentOptionsButtons = document.querySelectorAll('.payment-option');
const customDonationButton = document.querySelector('.custom-donation-button');
const customAmountInput = document.querySelector('.custom-amount');

donationButton.addEventListener('click', () => {
  paymentOptions.style.display = 'block';
});

paymentOptionsButtons.forEach(button => {
  button.addEventListener('click', () => {
    const amount = button.dataset.amount;
    const url = button.dataset.url + `?amount=${amount}`;
    window.open(url);
  });
});

customDonationButton.addEventListener('click', () => {
  const amount = customAmountInput.value;
  const url = document.querySelector('.donation-
